import React from "react";

export const Sobre = () => {
    return (
        <>
            <button onClick={() => window.history.back()}>
                voltar
            </button>
            <h1>Sobre</h1>
        </>
    );
}